[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[project]
name = "mybudget"
version = "0.1.0"
description = "My personal budget manager"
dependencies = ["pathlib"]